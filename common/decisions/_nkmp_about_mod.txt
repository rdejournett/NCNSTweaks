# decisions/your_decisions.txt

# Define your custom category
my_custom_decision_category = {
    # Optional: Conditions for the entire category to show up
    allowed = {
        # Checks only once at game start. Good for restricting to a specific country.
        original_tag = [YOUR_TAG] # e.g., original_tag = POL
    }
    
    visible = {
        # Checks continuously. What makes the category visible.
        always = yes 
    }

    # Your individual decision
    my_unique_decision_id = {
        # Set the icon (must be defined in a GFX file)
        icon = GFX_decision_political_concern 
        
        # How much it costs to take (usually Political Power)
        cost = 100 
        
        # Conditions for the decision to be available/selectable
        available = {
            has_war = no
            political_power > 50
        }
        
        # Conditions that make the decision visible in the UI
        # Only checks every game tick if 'allowed' was met.
        visible = {
            # Put less demanding checks here
            always = yes 
        }

        # The effect that happens when the player takes the decision
        complete_effect = {
            add_stability = 0.10 # Adds 10% stability
            add_political_power = 50 # Adds 50 Political Power
            set_country_flag = my_decision_taken
        }
        
        # Optional: AI behavior
        ai_will_do = {
            factor = 1 # AI will always take it if available
        }
    }
}