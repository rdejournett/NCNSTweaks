ENG_clear_naval_priorities = {
	allowed = {
		tag = GER
	}
	# Fixes the 'missing enable' error
	enable = {
		always = yes
	}

	# Fixes the 'missing abort' error
	abort = {
		always = no
	}
	# This multiplier scales the AI's "natural" desire for these missions.
	# Setting it to 0.0 effectively "mutes" the AI's autonomy for these tasks.

	ai_strategy = {
	    type = naval_mission_threshold
	    id = MISSION_PATROL
	    value = -100
	}	
}



GER_promote_home_naval_zones = {
	allowed = {
		tag = GER
	}
	
	enable = {
		has_war = yes
	}
	abort_when_not_enabled = yes
	
	# High Priority for Home Waters
	#ai_strategy = {
	#	type = naval_convoy_raid_region
#		id = 18 # English Channel (You can find IDs in map/strategicregions)
	#	value = 1000
	#}
	ai_strategy = {
		type = naval_convoy_raid_region
		id = 43 # western approaches
		value = 1000
	}
	ai_strategy = {
    	type = strike_force_home_base
    	id = 173 # north sea - north coast of germany
    	value = 1000
	}
	
	


}
