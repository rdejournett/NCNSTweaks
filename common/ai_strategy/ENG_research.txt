# AI Research Weights for the UK
ENG_naval_research_focus = {
	allowed = {
		original_tag = ENG
		has_dlc = "Man the Guns"
	}
	enable = {
		date < 1938.1.1
	}
	abort = {
		always = no
	}
	# THE FIX: Nested inside ai_strategy block
	ai_strategy = {
		type = research_weight_factor
		id = sonar # Correct ID for naval sonar/detection tech
		value = 500
	}
		ai_strategy = {
		type = research_weight_factor
		id = improved_sonar # Correct ID for naval sonar/detection tech
		value = 500
	}
		ai_strategy = {
		type = research_weight_factor
		id = advanced_sonar # Correct ID for naval sonar/detection tech
		value = 500
	}
	
}



ENG_change_ship_priority = {

	# Fixes the 'missing enable' error
	enable = {
		always = yes
	}

	# Fixes the 'missing abort' error
	abort = {
		always = no
	}
	
	#never build heavies
	ai_strategy = {
		type = role_ratio
		id = capital_ship
		value = 1
	}

	ai_strategy = {
		type = role_ratio
		id = carrier
		value = 1
	}
	
	# force creation of subs
	
	ai_strategy = {
        type = equipment_production_factor
        id = submarine
        value = 500
    }
	ai_strategy = {
		type = role_ratio
		id = naval_sub
		value = 1000
	}
	
	# force creations of DD
	ai_strategy = {
        type = equipment_production_factor
        id = screen_ship # Internal ID from script_enums
        value = 500   # +500% dockyard priority
    }
	
	ai_strategy = {
		type = role_ratio
		id = naval_screen
		value = 1000
	}
	# 2. Map the hulls specifically to those roles (Fixes "No Matching Role")
	ai_strategy = {
		type = role_target
		id = naval_screen
		value = 500
	}
	
	
	ai_strategy = {
		type = unit_ratio
		id = convoy
		value = 50
	}
	

}

#disable miner production

ENG_stop_mining_production = {
    allowed = { tag = ENG }
    enable = { always = yes }
    abort = { always = no }

	ai_strategy = {
        type = role_ratio
        id = naval_mine_layer 
        value = -99 # Effectively -100% desire
    }
	
		ai_strategy = {
        type = role_ratio
        id = naval_cruiser_light 
        value = -99 # Effectively -100% desire
    }
}
